---
title: .NET Getting Started
description: Learn about working with .NET with ease in Zerops.
---

import DocCardList from '@theme/DocCardList';
import Icons from '@theme/Icon';
import LargeCardList from '@site/src/components/LargeCardList';
import LargeCard from '@site/src/components/LargeCard';
import CustomCard from '@site/src/components/CustomCard';
import DeployButton from '@site/src/components/DeployButton';
import { Dropdown, DropdownItem } from '/src/components/Dropdown';

[.NET ↗](https://dotnet.microsoft.com/en-us/) is the free, open-source, cross-platform framework for building modern apps and powerful cloud services.

## 1. Try a Zerops Recipe

A **recipe** is a minimal working app in a public GitHub repository, already wired up with a `zerops.yml` — no setup needed on your part, just deploy and see it run.

<CustomCard title="Deploy the .NET recipe" emoji="🚀">
  A bare-bones .NET app with a single endpoint, connected to a PostgreSQL database. One click, no changes needed.

  <DeployButton link="dotnet" badgeWidth="200" />
</CustomCard>

<Dropdown>
<DropdownItem title="Import manually or fork the recipe">

Or import manually — log in to [Zerops GUI ↗](https://app.zerops.io), click **Import a project** in the Projects box, and paste this YAML ([source ↗](https://github.com/zeropsio/recipe-dotnet-hello-world/blob/main/import-project/description.yaml)):

```yaml
project:
  name: my-first-project
  # location: eu-central   # eu-central (EU, Prague, default) or us-east-1 (US East, New York)
services:
  - hostname: helloworld
    type: dotnet@latest
    minContainers: 1
    maxContainers: 3
    buildFromGit: https://github.com/zeropsio/recipe-dotnet-hello-world@main
    enableSubdomainAccess: true
```

Click **Import project** and wait for all pipelines to finish. The subdomain URL in the **IP Addresses & Public Routing** box should return `Hello, World!`.

To fork and build on top: [fork the repository ↗](https://github.com/zeropsio/recipe-dotnet-hello-world), replace `buildFromGit` with your fork's URL, then push — Zerops rebuilds on every push.

</DropdownItem>
</Dropdown>

---

## 2. Deploy your own project

Already have a .NET repo? Add a `zerops.yml` file to its root, then [create a .NET service](/dotnet/how-to/create) in Zerops and point it at your repository.

```yaml
zerops:
  - setup: app         # matches your service hostname in Zerops
    build:
      base: dotnet@latest
      buildCommands:
        - dotnet publish -c Release -o ./publish
      deployFiles:
        - ./publish
    run:
      base: dotnet@latest
      ports:
        - port: 5000
          httpSupport: true
      start: dotnet ./publish/app.dll
```

See the [full `zerops.yml` reference](/dotnet/how-to/build-pipeline) for all available options.

---

## Feature Highlights

<DocCardList
  colSize={4}
  items={[
    {
      type: 'link',
      href: '/dotnet/how-to/create',
      label: 'Create .NET service',
      customProps: {
        icon: Icons['dotnet'],
        description: 'Start with creating a .NET service using GUI or zCLI.',
      },
    },
    {
      type: 'link',
      href: '/dotnet/how-to/build-pipeline#add-zeropsyaml-to-your-repository',
      label: 'zerops.yml reference',
      customProps: {
        icon: Icons['puzzle'],
        description: 'Full reference for all zerops.yml options for .NET.',
      },
    },
    {
      type: 'link',
      href: '/dotnet/how-to/scaling',
      label: 'Scaling configuration',
      customProps: {
        icon: Icons['adjustments'],
        description:
          'Set up scaling so your app runs smoothly while using only the resources it needs.',
      },
    },
  ]}
/>

{" "}

<DocCardList
  colSize={6}
  items={[
    {
      type: 'link',
      href: '/dotnet/how-to/build-process#customize-build-environment',
      label: 'Customize build environment',
      customProps: {
        icon: Icons['sparkles'],
      },
    },
    {
      type: 'link',
      href: '/dotnet/how-to/customize-runtime',
      label: 'Customize runtime environment',
      customProps: {
        icon: Icons['sparkles-solid'],
      },
    },
  ]}
/>

## When in doubt, reach out

Don't know how to start or got stuck? You might not be the first one — join our community on Discord.

<DocCardList
  colSize={6}
  items={[
    {
      type: 'link',
      href: 'https://discord.com/invite/WDvCZ54',
      label: 'Discord',
      customProps: {
        icon: Icons['discord'],
        description:
          'Join our core team and Zerops community on Discord. Ask questions and share your tips with other members.',
      },
    },
  ]}
/>

## Popular Guides

<DocCardList
  colSize={6}
  items={[
    {
      type: 'link',
      href: '/references/cli',
      label: 'zCLI',
      customProps: {
        icon: Icons['command-line'],
        description:
          'Get even more out of Zerops with the zCLI command line tool.',
      },
    },
    {
      type: 'link',
      href: '/references/networking/vpn',
      label: 'Zerops VPN',
      customProps: {
        icon: Icons['globe-europe'],
        description: 'Connect to your services easily with Zerops VPN.',
      },
    },
  ]}
/>
