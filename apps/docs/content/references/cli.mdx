---
title: zCLI
---

zCLI is the Zerops command-line tool.

## Install zCLI
zCLI can be installed using npm: 

```sh
npm i -g @zerops/zcli
```

Currently, the zCLI is distributed for Linux (x86 & x64 architecture), macOS (x86-64 & AArch64 (M1) architecture) and Windows (x64 architecture).

To download the zCLI directly, use the [latest ↗](https://github.com/zeropsio/zcli/releases) release on GitHub.

### Windows

```powershell
irm https://zerops.io/zcli/install.ps1 | iex
```

This will be added to `C:\Program Files\`, `C:\Program Files (x86)\` or `C:\Windows\System32\`.

### Linux/MacOS

```sh
curl -L https://zerops.io/zcli/install.sh | sh
```

This will be added to `/usr/bin` or `/usr/local/bin`.

### NixOS

Note: Make sure `nix-command`& flakes are enabled if not you can also use `--extra-experimental-features 'nix-command flakes'` with the `nix` command.

- Clone the [zCLI repository](https://github.com/zeropsio/zcli)

```
git clone https://github.com/zeropsio/zcli
```

- cd into the root of the cloned repository and run `nix develop`
- run `nix build` to build the cli.
- zCLI's binary / execuetable  will be present in `./result/bin/zcli`.

## Create a personal access token
Personal tokens allow you to securely access Zerops using zCLI. You can create one or more personal tokens and manage them in Zerops GUI. 

To create a new personal access token open your account settings by clicking your avatar in the top right corner and choose **Access token management**.  Then create a new access token. 

<p align="center">
<img src="/img/screenshots/personal-access-tokens.webp" alt="image" width="95%" height="auto"/>
</p>

## Use the access token to login to zCLI
Login to zCLI using following command:
```sh
zcli login <token>
```
