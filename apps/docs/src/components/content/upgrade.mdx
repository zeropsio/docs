import { SetVar, Var, VarLink } from '/src/components/content/var';

You can upgrade or downgrade your <Var name="serviceDisplay" /> service to a different major <Var name="serviceDisplay" /> version by setting the <VarLink name="servicePath" path="/{{VAR}}/how-to/build-pipeline#base-1">`run.base`</VarLink> parameter in your `zerops.yaml`. When you [trigger a new pipeline](/{{VAR}}/how-to/trigger-pipeline), Zerops will start new runtime container(s) with the required <Var name="serviceDisplay" /> version. If you don't specify the `run.base` attribute in your `zerops.yaml`, Zerops keeps the current <Var name="serviceDisplay" /> version for your runtime.

If you want to build your application with a different major <Var name="serviceDisplay" /> version, change the <VarLink name="servicePath" path="/{{VAR}}/how-to/build-pipeline#base">`build.base`</VarLink> parameter in your `zerops.yaml`. The `build.base` is the required attribute.
